# Reproduction

[English] | [日本語](reproduction.ja.md)

The target quantities are the paper's four SEM path coefficients, the silence ⊥ voice independence, the latent-state means, and the SEM fit indices. The ABM-induced standardized path coefficients β̃ are estimated cross-sectionally with `fit-sem` and reconciled against the paper anchors.

## Anchors (paper §5)

| # | Quantity | Paper value | Reproduction target |
|---|----------|-------------|---------------------|
| B1 | β̃(ψ → fear) | −0.54 | sign match + 95% CI overlap |
| B2 | β̃(fear → acquiescent) | +0.86 | sign match + 95% CI overlap |
| B3 | β̃(fear → voice) | −0.23 | sign match + 95% CI overlap |
| B4 | β̃(acquiescent → silence) | +0.52 | sign match + 95% CI overlap |
| B5 | corr(silence, voice) | r = .02 (n.s.) | \|r\| < .10 |
| B6 | ABM-induced CFA CFI / GFI / RMSEA | .934 / .906 / .096 | same band ±0.02 |
| B7 | ABM-induced SEM GFI / RMSEA | .970 / .04 | RMSEA < .08, GFI > .95 |
| B8–B10 | ψ / fear / acquiescent M | .613 / .450 / .457 | initial-distribution means |
| B11 | mean silence rate | ≈ .38 | ±0.05 |
| B12 | mean voice volume | ≈ .45 | ±0.05 |
| B13–B14 | EN cultural ablation | comparison | β̃(fear→acquiescent) lower; ψ higher |

## Validity tiers (paper §6.3)

- **L1 face validity** — all four path signs match.
- **L2 magnitude** — 95% CI of β̃ overlaps the paper CI.
- **L3 fit** — ABM-induced SEM RMSEA < .08, CFI > .95.

Meeting all three is *full convergence*; one or two is *partial convergence*, in which case the sweep isolates the cause (network / prompt / initial distribution).

## How β̃ is estimated

`fit-sem` reads `agent_panel.csv`, drops the all-neutral t=0 step, time-averages each latent state and behaviour per agent, standardizes the columns, and fits the SEM with semopy (per-path OLS fallback). The four paths are reported with Wald 95% CIs; the SEM fit indices come from semopy's `calc_stats`. The silence/voice correlation (B5) is the Pearson r of the per-agent silence- and voice-frequency vectors.

## Running it

```bash
cargo run --release -- run --decision-mode rule --locale ja-JP --runs 30 --seed 2019
uv run fujimura-tools fit-sem --results-dir results/latest
uv run fujimura-tools reproduce --results-dir results/latest
```

`reproduce` prints the B1–B5 table and writes `paper_fig1_path_diagram.png` + `reproduction_report.json`. The rule-mode path reproduces all four SEM path signs; magnitudes land in the right ballpark and the SEM fit indices fall in the paper's quality band. The silence/voice correlation is small but mildly negative — an honest near-miss on B5, reflecting that fear simultaneously suppresses voice and (via acquiescence) drives silence, which is the paper's own causal structure.

---
*This file was generated by Claude Code.*
