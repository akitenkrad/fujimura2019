# 再現

[English](reproduction.md) | [日本語]

再現対象は原著の 4 SEM パス係数，沈黙 ⊥ 発言の独立性，潜在状態の平均，SEM 適合度である．ABM 由来の標準化パス係数 β̃ を `fit-sem` で横断推定し，原著アンカーと照合する．

## アンカー（原著 §5）

| # | 量 | 論文値 | 再現目標 |
|---|----|--------|---------|
| B1 | β̃(ψ → 怖れ) | −0.54 | 符号一致 + 95%CI 重なり |
| B2 | β̃(怖れ → 黙従) | +0.86 | 符号一致 + 95%CI 重なり |
| B3 | β̃(怖れ → 発言) | −0.23 | 符号一致 + 95%CI 重なり |
| B4 | β̃(黙従 → 沈黙) | +0.52 | 符号一致 + 95%CI 重なり |
| B5 | corr(沈黙, 発言) | r=.02（n.s.） | \|r\| < .10 |
| B6 | ABM-induced CFA CFI / GFI / RMSEA | .934 / .906 / .096 | 同帯 ±0.02 |
| B7 | ABM-induced SEM GFI / RMSEA | .970 / .04 | RMSEA < .08, GFI > .95 |
| B8–B10 | ψ / 怖れ / 黙従 M | .613 / .450 / .457 | 初期分布平均 |
| B11 | 平均沈黙率 | ≈ .38 | ±0.05 |
| B12 | 平均発言率 | ≈ .45 | ±0.05 |
| B13–B14 | EN 文化 ablation | 比較 | β̃(怖れ→黙従) 低下；ψ 上昇 |

## 妥当性の 3 階層（原著 §6.3）

- **L1 face validity** — 4 パスすべて符号一致．
- **L2 magnitude** — β̃ の 95%CI が原著 CI と重なる．
- **L3 fit** — ABM-induced SEM の RMSEA < .08, CFI > .95．

3 階層すべて満たせば *full convergence*，1〜2 のみなら *partial convergence*．後者は sweep で原因（ネットワーク / プロンプト / 初期分布）を切り分ける．

## β̃ の推定法

`fit-sem` は `agent_panel.csv` を読み，全 Neutral の t=0 を除き，エージェントごとに潜在状態・行動を時間平均し，列を標準化して semopy で SEM をフィットする（semopy 不在時は per-path OLS）．4 パスを Wald 95%CI 付きで報告し，SEM 適合度は semopy の `calc_stats` から得る．沈黙／発言相関（B5）はエージェント単位の沈黙頻度・発言頻度ベクトルの Pearson r．

## 実行

```bash
cargo run --release -- run --decision-mode rule --locale ja-JP --runs 30 --seed 2019
uv run fujimura-tools fit-sem --results-dir results/latest
uv run fujimura-tools reproduce --results-dir results/latest
```

`reproduce` は B1–B5 表を表示し，`paper_fig1_path_diagram.png` + `reproduction_report.json` を書く．ルールモード経路は 4 SEM パスの符号をすべて再現し，効果量は妥当な範囲に収まり，SEM 適合度は原著の品質帯に入る．沈黙／発言相関はわずかに負で，B5 の正直なニアミスである — これは怖れが発言を抑制しつつ（黙従経由で）沈黙を駆動するという原著自体の因果構造を反映している．

---
*This file was generated by Claude Code.*
